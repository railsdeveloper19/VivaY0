$("#comment_#{@post.id}").html("#{escape_javascript(render :partial => 'posts/comment',  :locals => {:post => @post, :comments => @comments} )}");
$("#comment_size").text('#{!@comments.blank? ? @post.comments.size : 0}');
